<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="attendanceDetailItem"
            type="com.alurwa.berkelas.model.AttendanceDetailItem" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="@dimen/spacing_normal"
        android:paddingTop="12dp"
        android:paddingEnd="@dimen/spacing_normal"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:singleLine="true"
            android:text="@{attendanceDetailItem.name}"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:textColor="?android:attr/textColorPrimary"
            tools:text="Rosamala" />

        <ImageView
            android:id="@+id/img_check"
            imgAttendanceCheck="@{attendanceDetailItem.check}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_normal"
            app:srcCompat="@drawable/ic_round_check_circle_24" />
    </LinearLayout>
</layout>
